# Speech Classifier


This is an app which classifies between normal and hyper nasal speech. To run this app the input must be a wav file and need to be preprocessed for the model. After gathering the wav file a CNN model will be run on the audio file and classify the input. 

### Usage
```bash
python recommender.py
```